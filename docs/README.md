## OpusReader Documentation

Build the HTML docs via the root make target:

```bash
make docs
```

That regenerates the Doxygen XML (`docs_generated/xml`) and renders the Sphinx
site into `docs/build/html`.
